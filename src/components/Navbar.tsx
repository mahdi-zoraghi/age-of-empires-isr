import styles from "../styles/Navbar.module.css";

const Navbar = () => {
  return (
    <nav>
      <section>
        <h1>Logo</h1>
      </section>
    </nav>
  );
};

export default Navbar;
